import 'package:flutter/material.dart';

final ThemeData mainTheme = ThemeData.dark().copyWith(
  colorScheme: ColorScheme.fromSwatch()
      .copyWith(primary: Colors.amber[800], secondary: Colors.amberAccent[700]),
);
